def compareNumber(a, b) {
    if (a > b) {
        println "The number ${a} is greater than ${b}"
    }
    else if(b > a){
        println "The number ${b} is greater than ${a}"
    else{
        println "Both Numbers are equal"
    }
}
